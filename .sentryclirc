# Sentry CLI Configuration
[defaults]
# Org de Sentry
org = digitaistudios

# Projecte de Sentry
project = crims-frontend

# DSN (sobreescriu per entorn)
dsn = https://xxx@sentry.io/xxx

# Environment
environment = development

# Auth token via env var (SENTRY_AUTH_TOKEN)
